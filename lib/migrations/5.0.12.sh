#!/bin/bash
#############################################
# Cipi Migration 5.0.12 — noop
#
# Safer php switch (API pool migrate + socket check + rollback)
# lives in lib/php.sh (copied by self-update).
#############################################

set -e

echo "Migration 5.0.12 — nothing to migrate (php.sh updated by self-update)"
echo "Migration 5.0.12 complete"
